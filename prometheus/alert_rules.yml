groups:
  - name: app_alerts
    rules:
      - alert: HighErrorRate
        expr: rate(app_errors_total[1m]) > 5
        for: 0m
        labels:
          severity: critical
        annotations:
          summary: "CRITICAL: High error rate detected"
          description: "Error rate has exceeded 5 errors per minute"